{
    "private": true,
    "scripts": {
        "build": "nitro build",
        "predev": "npm install && prisma migrate deploy",
        "dev": "nitro dev",
        "prepare": "nitro prepare && prisma generate",
        "preview": "node .output/server/index.mjs",
        "update": "nitro task run update",
        "migrate:dev": "prisma migrate dev",
        "migrate": "prisma migrate deploy"
    },
    "devDependencies": {
        "prisma": "^5.19.1"
    },
    "dependencies": {
        "@prisma/client": "^5.19.1",
        "bottleneck": "^2.19.5",
        "jose": "^5.8.0",
        "nitropack": "latest",
        "ohash": "^1.1.3",
        "zod": "^3.23.8"
    }
}
